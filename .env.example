# IKG sudio dataset Manager Configuration

# PostgreSQL connection
DATABASE_URL=postgresql://fiftyone:fiftyone@postgres:5432/fiftyone_manager

# Base path for all datasets
DATASET_BASE_PATH=/data/datasets

# Port range for FiftyOne instances
PORT_START=5151
PORT_END=5160

# Manager API port
MANAGER_PORT=5000

# Public address for accessing the manager and label editor
# Use the IP/hostname that users access FiftyOne from
# Examples: localhost, 192.168.1.100, myserver.local
PUBLIC_ADDRESS=localhost

# Default IoU threshold for duplicate detection (0.0 - 1.0)
DEFAULT_IOU_THRESHOLD=0.25

# Default debug mode (true/false)
DEFAULT_DEBUG_MODE=false

# Health check interval in milliseconds (how often to check if FiftyOne service is responding)
HEALTH_CHECK_INTERVAL=5000

# Health check timeout in milliseconds (how long to wait for FiftyOne service response)
HEALTH_CHECK_TIMEOUT=3000

# Label editor preload configuration
# Number of adjacent images to preload around the current image
LABEL_EDITOR_PRELOAD_COUNT=25

# Viewer image loading concurrency
# Max number of thumbnail requests loaded in parallel when opening Viewer
VIEWER_IMAGE_LOADING_BATCH_COUNT=200

# Thumbnail JPEG quality (1-100, default 70)
THUMBNAIL_QUALITY=50

# API access log level: info | debug | silent
# info = method/path/status/duration/IP, debug adds User-Agent
API_LOG_LEVEL=info

# OBB Creation Mode Configuration
# Comma-separated list of available OBB creation modes for admin selection
# Valid values: rectangle, 4point
# Default: rectangle,4point (both modes available)
AVAILABLE_OBB_MODES=rectangle,4point

# Duplicate Handling Configuration
# Rules for handling duplicates based on dataset path patterns (JSON array)
# Each rule: pattern (string to match in path), action (skip|move|delete), labels (0=all), priority (lower=higher)
# Example:
# DUPLICATE_RULES='[
#   {"pattern": "invalid", "action": "skip", "labels": 0, "priority": 1},
#   {"pattern": "dice", "action": "delete", "labels": 3, "priority": 2},
#   {"pattern": "test", "action": "move", "labels": 0, "priority": 3}
# ]'
DUPLICATE_RULES=

# Default action when no pattern matches: skip | move | delete
DUPLICATE_DEFAULT_ACTION=move
